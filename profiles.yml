# Local portfolio profile — DuckDB in-process engine.
# Production runs on Google BigQuery (dataset layers raw/staging/intermediate/mart)
# triggered hourly by Cloud Scheduler; only the profile changes, not the models.
cybryne_logistics:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: cybryne_logistics.duckdb
      threads: 4
